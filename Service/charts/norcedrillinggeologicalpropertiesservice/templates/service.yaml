apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillinggeologicalpropertiesservice.fullname" . }}
  labels:
    {{- include "norcedrillinggeologicalpropertiesservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillinggeologicalpropertiesservice.selectorLabels" . | nindent 4 }}
